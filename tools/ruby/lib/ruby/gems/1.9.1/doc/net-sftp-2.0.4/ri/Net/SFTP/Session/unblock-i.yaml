--- !ruby/object:RDoc::RI::MethodDescription 
aliases: []

block_params: 
comment: 
- !ruby/struct:RDoc::Markup::Flow::P 
  :body: "Removes a previously created byte-range lock. This operation is only available in protocol versions 6 and higher. The <tt>offset</tt> and <tt>length</tt> parameters must exactly match those that were given to #block when the lock was acquired."
full_name: Net::SFTP::Session#unblock
is_singleton: false
name: unblock
params: |
  unblock(handle, offset, length) -> request
  unblock(handle, offset, length) { |response| ... } -> request

visibility: public
